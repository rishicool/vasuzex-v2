# ===================================
# CENTRALIZED NODE_MODULES (Like Laravel vendor/)
# Only ONE node_modules at workspace root
# ===================================

# Use hoisted node linker (flat, like npm - single node_modules)
node-linker=hoisted

# Shared lockfile for entire workspace
shared-workspace-lockfile=true

# Strict peer dependencies (false for flexibility)
strict-peer-dependencies=false

# Auto install peer dependencies
auto-install-peers=true
